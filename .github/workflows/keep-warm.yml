name: Keep Railway Warm
on:
  schedule:
    - cron: "*/10 * * * *"  # Every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit keep-warm ping
        run: curl -s https://signupassist-mcp-production.up.railway.app/ping
