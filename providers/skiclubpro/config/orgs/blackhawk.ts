/**
 * Blackhawk Ski Club Organization Configuration
 */

import { OrgConfig } from '../../types';

export const BLACKHAWK_CONFIG: OrgConfig = {
  orgRef: 'blackhawk-ski-club',
  displayName: 'Blackhawk Ski Club',
  
  // Org-specific overrides (if needed)
  selectorOverrides: {
    // Add any Blackhawk-specific selector overrides here
    // For now, using provider defaults
  },
};
