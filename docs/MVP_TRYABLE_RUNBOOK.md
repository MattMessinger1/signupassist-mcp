# SignupAssist Tryable MVP Runbook

This runbook is superseded.

Use `docs/SIGNUPASSIST_PRODUCTION_RUNBOOK.md` for the current SignupAssist production readiness, web golden-path, ChatGPT approval-safety, evidence-capture, and stabilization workflow.

Historical note:

- This file described an earlier parent-tryable Railway/Supabase smoke path.
- It referenced the old Railway URL `https://signupassist-mcp-production.up.railway.app`.
- The current canonical production URL is `https://signupassist.shipworx.ai/`.
- The current front door for the web app proof is `/activity-finder`, followed by `/autopilot?intent=<id>` and `/dashboard`.

Do not use this file for release decisions.
